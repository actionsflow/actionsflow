on:
  {{snakeCase name}}:
jobs:
  print:
    name: Print
    runs-on: ubuntu-latest
    steps:
      - name: Print Outputs
        env:
          json: $\{{toJSON(on.{{snakeCase name}}.outputs)}}
        run: |
          echo json: $json
